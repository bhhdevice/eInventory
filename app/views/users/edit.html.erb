<h1>Editing <%= @user.full_name %></h1>
<% if @user.locked_at? %>
  <h5 style="color:red">ACCOUNT LOGIN DISABLED!</h5>
<% end %>

<%= render 'form', user: @user %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
